# Break & Continue

## What I Learned

- `break` stops the loop immediately.
- `continue` skips the current iteration.
- `break` exits the loop completely.
- `continue` moves to the next iteration.
- Both are useful for controlling loop behavior.